---
title: "Accidental debug statements"
description: "Temporary debugging output that shipped to production"
icon: "bug"
iconType: "duotone"
---

This reference describes the accidental debug statements policy category.

## Definition

Accidental debug statements are developer-authored messages that do not describe application behavior, system state, or user activity. Examples include `console.log("here")`, `print("debug")`, and `logger.info("asdf")`.

Severity does not determine this category. Accidental debug statements can appear at `DEBUG`, `INFO`, or any other severity level. The message content is the primary signal.

## Signals

- Placeholder messages such as `got here`, `hello world`, `testing`, or `asdf`.
- Explicit cleanup notes such as `TODO remove`.
- Bare variable dumps such as `x = 42`.
- Profanity or informal phrases used as temporary debugging markers.
- Messages with no stable operational meaning.

## Example

<Tabs>
  <Tab title="Before">
    ```json
    {
      "@timestamp": "2024-01-15T10:30:00Z",
      "service.name": "checkout-api",
      "severity_text": "DEBUG",
      "body": "got here"
    }
    ```
  </Tab>
  <Tab title="After">
    Dropped entirely.
  </Tab>
</Tabs>

Tero generates a policy to drop this specific log event:

```yaml
id: drop-debug-got-here-checkout-api
name: Drop got-here debug log from checkout-api
description: Accidental debug statement that shipped to production.
log:
  match:
    - resource_attribute: service.name
      exact: checkout-api
    - log_field: body
      exact: "got here"
  keep: none
```

## Recommended enforcement

<Card title="Open PRs" icon="code-pull-request" href="/policies/enforcement/open-prs" horizontal>
  Remove accidental debug statements from the codebase.
</Card>

Source enforcement is recommended because the log statement itself should not exist in production code.

## Detection notes

- Tero evaluates message content rather than severity.
- Common debug phrases, random character strings, cleanup notes, and bare variable dumps can be flagged.
- Tero is conservative for this category and flags logs that are unambiguously accidental.
- Legitimate verbose logging is not included in this category when the message has operational meaning.
